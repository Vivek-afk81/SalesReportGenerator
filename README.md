# SalesReportGenerator
SalesReportGenerator is a Python-based command-line tool that reads product IDs from a text file, processes sales data using predefined product details, and generates reports in CSV, JSON, and Excel formats. It's perfect for quickly turning raw sales entries into structured, exportable reports.
